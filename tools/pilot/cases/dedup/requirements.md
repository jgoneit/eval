Repair `Unique(items []string) []string` in `task.go`.

Return each distinct string exactly once, preserving the order of its first appearance. Empty strings are valid values. Empty or nil input produces an empty result. Do not mutate the input slice, including its element order, and do not allow later writes to the returned slice to change the input. Keep the public function signature and package unchanged.
